import {  } from "./action-types.js"

const initialState={
    myFavorites:[],
    allCharactersFav:[]
};

const reducer = (
    state = initialState,
    { type, payload })=>{

    switch(type){
       
        
    }
};

export default reducer